version: "3"
services:
  secure-open:
    build: .
    image: lapwat/secure-open
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ${ABSOLUTE_PATH}:/app/data/${NAME}:${READOPTION}
    ipc: host
    environment:
      - DISPLAY=unix${DISPLAY}
    devices:
      - /dev/snd
      - /dev/dri
